# platform :ios, '17.6'
use_frameworks!

target 'ElementX' do
  # pod 'JitsiMeetSDK'
  # Add the dependencies that JitsiMeetSDK needs
  pod 'Giphy', '2.2.12'  # Using the exact same version
  pod 'JitsiWebRTC', '~> 124.0'
end